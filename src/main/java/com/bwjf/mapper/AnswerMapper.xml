<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bwjf.mapper.AnswerMapper">

    <!-- 添加一条记录 -->
    <insert id="insert" parameterType="Answer">
        insert into answer(problemid,respondentno,optionsid,content)
        values(#{problemId},#{respondentNo},#{optionsId},#{content})
    </insert>

</mapper>